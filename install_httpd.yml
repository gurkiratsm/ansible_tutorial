---

- hosts: all
  become: true
  # gather_facts: false
  tasks:

    - name: update repository index and install apache and php package 
      package:
        name: 
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: latest
        update_cache: yes
    